# components.md — module registry

Consult this before touching or adding code: what exists, its contract, where it's tested. One row per exported unit; signatures abbreviated (see the source JSDoc for full shapes). "—" in Tested = deliberately untested glue (see `testing.md`).

## src/lib — the pure brain (no React / chrome / fetch / DOM)

### lib/counting (`twitter.ts`)

| Export                 | Contract                                                                                                                     | Tested in         |
| ---------------------- | ---------------------------------------------------------------------------------------------------------------------------- | ----------------- |
| `weightedLength(text)` | X's official weighted count via `twitter-text` (URLs = 23, some chars = 2). Handles the lib's CJS/ESM dual shape internally. | `twitter.test.ts` |
| `isOver280(text)`      | `weightedLength > X_HARD_LIMIT` — the deterministic cap gate                                                                 | `twitter.test.ts` |
| `X_HARD_LIMIT`         | `280`                                                                                                                        | `twitter.test.ts` |

### lib/exclusion

| Export                                         | Contract                                                                                                                                                                                                                              | Tested in            |
| ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- |
| `detectEmDash(text)` → `Span[]`                | Flags U+2014 only (en-dash deliberately allowed)                                                                                                                                                                                      | `structural.test.ts` |
| `detectSmartQuotes(text)` → `Span[]`           | Flags U+2018/2019/201C/201D                                                                                                                                                                                                           | `structural.test.ts` |
| `detectStaccato(text)` → `Span[]`              | ≥3 consecutive sentences of ≤4 words → one span per run. Pragmatic sentence segmenter ( `.!?` + whitespace); "Dr." false-positives accepted                                                                                           | `structural.test.ts` |
| `detectAiColon(text)` → `Span[]`               | Sentence-opening label-colon ("The result: …"). Narrow by construction: 1–4 letter-only words before the colon, space+letter after on the same line, no comma/colon in the clause — times, ratios, list lead-ins can never match      | `structural.test.ts` |
| `detectDoNotSay(text, entries)` → `Span[]`     | Whole-word, case-insensitive, multi-word-sequence banlist matcher; tokens = `\p{L}\p{N}'_`; empty entries skipped; `entry` recorded per hit                                                                                           | `doNotSay.test.ts`   |
| `autoFix(text, {fixEmDash, fixSmartQuotes})`   | Mechanical rewrite: em dash (+surrounding ws) → `", "`, curly → straight. Returns `{text, appliedFixes: Span[]}` with original-text offsets                                                                                           | `autoFix.test.ts`    |
| `describeViolations(violations)` (describe.ts) | Human-facing summary for the compose UI's violation note — counted structural rules in fixed order, banned words quoted by entry (deduped); '' when empty. Deliberate SIBLING of the model-facing `summarizeViolations`, never shared | `describe.test.ts`   |
| `checkExclusions(text, settings)`              | Runs enabled detectors + banlist; returns `{violations: Span[]}` sorted by start                                                                                                                                                      | `check.test.ts`      |
| `hasRepairableViolations(result)`              | `violations.length > 0`                                                                                                                                                                                                               | `check.test.ts`      |
| `Span` / `RuleId` (types.ts)                   | `{start, end, rule, matchedText, entry?}` — JS code-unit offsets into the checked text                                                                                                                                                | (via consumers)      |

### lib/prompt

| Export                                                                                                                                                                                                                             | Contract                                                                                                                                                                                                                                                               | Tested in                                           |
| ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------- |
| `fillSlots(body, values)`                                                                                                                                                                                                          | `{{slot}}` substitution in one body string; whitespace-tolerant; unknown slots → `''`                                                                                                                                                                                  | `template.test.ts`                                  |
| `renderTemplate(template, values)` → `RenderedPrompt`                                                                                                                                                                              | Fills both bodies with the same values → `{system, user}`, each trimmed. The role boundary is structural — the two `PromptTemplate` fields — never a marker inside a string                                                                                            | `template.test.ts`                                  |
| `validateTemplate(template)`                                                                                                                                                                                                       | Drift report across **both** bodies: `{declaredButUnused, usedButUndeclared}` — warnings, never crashes                                                                                                                                                                | `template.test.ts`                                  |
| `extractSlotNames(body)`                                                                                                                                                                                                           | De-duped slot names in body order. **The only slot parser** — UI must use this, not a local regex                                                                                                                                                                      | `template.test.ts`                                  |
| `DEFAULT_PROMPT_TEMPLATES` (defaults.ts)                                                                                                                                                                                           | **Single source of truth** for the four default templates (`reply`/`post`/`refine`/`thread`), each a `{system, user}` pair — system carries role + `<precedence>`/`<style_guide>`/`<exclusions>`, user the per-call blocks. `DEFAULT_SETTINGS` imports it — never copy | `defaults.test.ts` (identity, drift, role-boundary) |
| `GENERATION_PRECEDENCE` / `REFINE_PRECEDENCE` / `THREAD_PRECEDENCE`                                                                                                                                                                | Code-supplied authority order filling `{{precedence}}` — not user-editable prose; still visible via the inspector                                                                                                                                                      | `defaults.test.ts`                                  |
| `INTENT_FRAMING` / `IntentShape`                                                                                                                                                                                                   | Fragments/prose framing lines filling `{{intentFraming}}`, keyed by `classifyIntentShape`                                                                                                                                                                              | (via `assemble.test.ts`)                            |
| `TIGHTEN_INSTRUCTION` / `buildRepairInstruction(summary)` / `POLISH_INSTRUCTION` / `REFIT_INSTRUCTION`                                                                                                                             | Code-supplied `{{instruction}}` values: tighten + exclusion-repair (pipeline backstops), polish (length-preserving), refit (content-preserving ≤280)                                                                                                                   | `defaults.test.ts`                                  |
| `formatExamples(items)`                                                                                                                                                                                                            | `{{voiceExamples}}` value, each item in its own `<example>` tag — tags, not numbering/blank lines, are the boundaries (tweets contain both); empty → cold-start line                                                                                                   | `defaults.test.ts`                                  |
| `buildAspirationalBlock(items)`                                                                                                                                                                                                    | Whole `<aspirational_examples>` block, or `''` to collapse (pool is empty until Phase 5 favorites)                                                                                                                                                                     | `defaults.test.ts`                                  |
| `buildExclusionInstructions(settings)`                                                                                                                                                                                             | Prompt-side "patterns to avoid" lines from active rules + banlist                                                                                                                                                                                                      | `defaults.test.ts`                                  |
| `buildCharConstraintInstruction({charCap, softCapChars})`                                                                                                                                                                          | Hard-280 or soft-cap `{{length}}` text (prompt-side only)                                                                                                                                                                                                              | `defaults.test.ts`                                  |
| `buildThreadContextBlock(grandparentText)`                                                                                                                                                                                         | Whole `<thread_context>` block, or `''` to collapse                                                                                                                                                                                                                    | `defaults.test.ts`                                  |
| `buildThreadExamplesBlock` / `buildThreadConstraintInstruction` / `buildRepackInstruction` / `buildTightenSegmentsInstruction` / `buildCountNudgeLine` / `THREAD_FORMAT_REMINDER` / `THREAD_CAP_LINE` / `THREAD_REFIT_INSTRUCTION` | Phase 10 thread prompt pieces: per-thread `<thread_example>` tags with `1/ 2/ …` segment markers inside (collapses empty); the ≈N + per-post cap text; the repack / tighten-posts / count-nudge backstop instructions; the `---` output-format reminder                | `defaults.test.ts`                                  |
| `REWRITE_POST_INSTRUCTION` / `buildScopedThreadInstruction(ordinal, instruction)`                                                                                                                                                  | Scoped refines: the code-supplied fresh-take instruction + the revise-ONE-post wrapper (asks the whole thread back; the pipeline enforces the splice)                                                                                                                  | `defaults.test.ts`                                  |
| `assembleInitialPrompt(request, settings, pools)` (assemble.ts)                                                                                                                                                                    | Fills the mode's template from settings + `{voice, aspirational}` pools → `RenderedPrompt`; placeholder lines for empty styleGuide/bullets; reply mode adds targetText/threadContext; picks the intent framing per call                                                | `assemble.test.ts`                                  |
| `assembleRefinePrompt(settings, draft, instruction)`                                                                                                                                                                               | The single refine path for chip / more-less / repair / tighten → `RenderedPrompt` carrying the same system voice anchor as generation (the voice-blind-refinement fix)                                                                                                 | `assemble.test.ts`                                  |
| `classifyIntentShape(bullets)` → `IntentShape`                                                                                                                                                                                     | `fragments` iff ≥2 non-empty lines or one line starting `-`/`*`/`•`; else `prose` (incl. empty)                                                                                                                                                                        | `assemble.test.ts`                                  |
| `composeMoreLessInstruction(more, less)`                                                                                                                                                                                           | Folds the panel's more/less fields into one instruction string; both blank → `''` (caller guards)                                                                                                                                                                      | `assemble.test.ts`                                  |
| `summarizeViolations(violations)`                                                                                                                                                                                                  | Repair-instruction bullet block: one line per fired structural rule + deduped banlist entries                                                                                                                                                                          | `assemble.test.ts`                                  |
| `escalateChipInstruction(instruction, intensity)`                                                                                                                                                                                  | Press 1 = bare; 2/3 = escalating preamble; 4+ = numbered "MAXIMUM intensity"                                                                                                                                                                                           | `assemble.test.ts`                                  |

### lib/sampling (`selectExamples.ts`)

| Export                                                                      | Contract                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Tested in                                   |
| --------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------- |
| `selectExamples(mode, context, library, options)` → `{aspirational, voice}` | **The CLAUDE.md §8 retrieval seam**, now the full Concept A model: guaranteed star pool (≤ `starCount`, capped at `floor(poolSize/2)`, excluded from voice — no item twice) + curated-first/archive-top-up tiers per `curatedShare`. With `options.bundleMemberIds` (Phase 6) the resolved members ARE the voice pool — verbatim, bundle order, no shuffle / mode filter / cap / top-up; stars still ride on top minus members. Thread mode (Phase 10): thread exemplars fill first on a TWEET-EQUIVALENT budget (a thread debits poolSize by its segment count; starred threads guaranteed-first; first thread always taken; non-fitting skipped individually), posts top up via the shared tier math, aspirational = starred posts, `threads[]` on the result (empty elsewhere). Injectable `rng`; `context` accepted-and-ignored for future retrieval; never mutates input | `selectExamples.test.ts` (33 deterministic) |

### lib/thread (`parse.ts`)

| Export                                       | Contract                                                                                                                                                                                          | Tested in       |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------- |
| `parseThreadSegments(raw)`                   | THE `---` wire format's parser (model output, refine drafts, stored text, pastes): line-anchored split, trims, drops empties, strips narrow accidental `1/`-numbering; no delimiter → one segment | `parse.test.ts` |
| `joinSegments(segments)`                     | The canonical inverse; round-trip pinned                                                                                                                                                          | `parse.test.ts` |
| `THREAD_DELIMITER` / `DEFAULT_THREAD_TARGET` | `'---'`; ≈5 — the per-composition stepper default (code constant, deliberately not a settings field)                                                                                              | `parse.test.ts` |

### lib/bundles (`resolveMembers.ts`)

| Export                                                                 | Contract                                                                                                                                                                      | Tested in                |
| ---------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------ |
| `resolveBundleMembers(memberIds, library)` → `{members, missingCount}` | Bundle-order resolution against the library; dangling ids drop with an HONEST count (never absorbed silently); a duplicated id resolves once, at first position (defensive)   | `resolveMembers.test.ts` |
| `appendBundleMember(bundle, itemId)` → `Bundle`                        | Auto-filing growth: appends at the end (series order); returns the input BY IDENTITY when already a member so callers skip the write (the `mergeLibraryDuplicate` convention) | `resolveMembers.test.ts` |
| `moveBundleMember(bundle, itemId, dir, presentIds)` → `Bundle`         | One VISIBLE step up/down: swaps with the nearest neighbor that still resolves, so a dangling id keeps its slot and never swallows the move; identity return at the ends       | `resolveMembers.test.ts` |

### lib/screening (`predicates.ts`) — DORMANT until Phase-2 import (nothing imports it; keep tested)

| Export                       | Contract                                                           | Tested in            |
| ---------------------------- | ------------------------------------------------------------------ | -------------------- |
| `isEmojiOnly(text)`          | True iff trimmed text is pictographic/flags/modifiers + whitespace | `predicates.test.ts` |
| `isSingleWord(text)`         | Exactly one whitespace-delimited token                             | `predicates.test.ts` |
| `isBelowMinChars(text, min)` | Trimmed code-unit length < min                                     | `predicates.test.ts` |

### lib/voice

| Export                                          | Contract                                                                                                                        | Tested in                |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ------------------------ |
| `validateAuthor(tweetHandle, configuredHandle)` | **The hard capture filter.** Case-insensitive, `@`/whitespace-tolerant, exact match; empty on either side rejects               | `validateAuthor.test.ts` |
| `classifyType(context)` → `'post'\|'reply'`     | Signal precedence: `hasReplyContextNode` → `inReplyToStatusId` → `isPrecededByParentArticle` → post. User can override per item | `classifyType.test.ts`   |

### lib/url (`isXPageUrl.ts`)

| Export            | Contract                                                                                                                                                       | Tested in            |
| ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- |
| `isXPageUrl(url)` | https + the four X hostnames; undefined/unparseable → false (chrome hides non-permitted URLs, so "can't see it" = "not on X"). Match-pattern twin of `X_HOSTS` | `isXPageUrl.test.ts` |

### lib/draft (`lifecycle.ts`, `commit.ts`)

| Export                                                     | Contract                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Tested in           |
| ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------- |
| `reduceDraftLifecycle(state, event)`                       | **The draft state machine** (empty → generating → active → committed), now MULTI-POST: `content.posts[]` each with text/violations/`copied` (length 1 for singles). Owns stale-seq gating, both undo scopes, per-post hand-edit semantics (clears THAT post's violations + copied flag, un-commits), THE commit rule (`post-copied(i)`: every post copied since its last change ⇒ committed — singles are the N=1 case), the `done` exit (committed-only: draft + workbench clear into the timed-undo snapshot — completion, not discard), `post-replaced` (a scoped refine splices ONE post — only its copied flag resets; the global one-level undo reverses it exactly), `targetCount` carry-over, and the bundle-seed provenance | `lifecycle.test.ts` |
| `INITIAL_DRAFT_LIFECYCLE`                                  | The empty workbench                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | `lifecycle.test.ts` |
| `applyBulletPrefixes` / `stripBulletPrefixes` (bullets.ts) | Bullet-mode text transforms (real `•` glyphs; upgrades typed `-`/`*`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | `bullets.test.ts`   |
| `onDraftCommit(listener)` / `emitDraftCommit(commit)`      | The corpus half of "done" — fired at the committed TRANSITION (all posts copied); Phase 4's loop subscribes. `DraftCommit` carries `{text, segments, mode, handEdited, seedBundleId, committedAt}` — threads ship their ordered segments, text is the joined wire format. Throwing listeners can't starve siblings                                                                                                                                                                                                                                                                                                                                                                                                                   | `commit.test.ts`    |

### lib/replyContext (`merge.ts`)

| Export                                           | Contract                                                                                                                                                                                                                         | Tested in                                                                                                                                         |
| ------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `mergeReplyContextSelection(existing, incoming)` | Same tweet (status-id equality, else normalized text) → field-wise merge: fresh reading wins, existing fills gaps, media flag ORs. Different tweet / no lock → incoming. Stops X's metadata-poor modal copies degrading the lock | `merge.test.ts` ; `normalizeTweetText` exported as the one text-identity normalizer ; `isTruncatedRenderingOf` (truncation-gated prefix identity) |

### lib/library (`dedupe.ts`)

| Export                                          | Contract                                                                                                                                                                                                                                                                                                                                                            | Tested in        |
| ----------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `findLibraryDuplicate(items, {statusId, text})` | Concept A identity, tiered strongest-first: record id → thread SEGMENT status id → whole text → segment text (all via the shared `normalizeTweetText`) — capturing one segment of a saved thread finds the thread                                                                                                                                                   | `dedupe.test.ts` |
| `mergeLibraryDuplicate(existing, incoming)`     | Precedence `manual > shipped > archive`: lower never downgrades (returns existing by identity → callers skip the write); equal-or-higher updates IN PLACE — fresh source/type/text/segments win, metadata enriches, storage id stable. Threads: a single never merges INTO a thread (identity return); a thread upgrades a previously-captured single root in place | `dedupe.test.ts` |
| `SOURCE_PRECEDENCE`                             | The numeric ranking Phase 7's archive import inherits                                                                                                                                                                                                                                                                                                               | `dedupe.test.ts` |
| `matchesSearch(text, query)`                    | The Voice-screen live-search predicate: token-AND, case-insensitive substring per token; empty query matches all. No fuzziness/ranking on purpose — legible whittling                                                                                                                                                                                               | `search.test.ts` |

### lib/overlay (`visibility.ts`)

| Export                            | Contract                                                                                                                                                                                                                                                                                                                                                    | Tested in            |
| --------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- |
| `decideOverlayVisibility(inputs)` | **The single on-page render decision.** `{showLock, showPreview}` from `{panelOpen, captureMode, xModalOpen, hasLockTarget, hoveringTweet, hoveredInModal}` — panel closed → nothing; the lock paints whenever mode + lock exist (layer discipline lives in the shell's scoped article search); modal open scopes the preview to modal-resident tweets (§6) | `visibility.test.ts` |

### lib/format (`relativeTime.ts`)

| Export                               | Contract                                                                                                               | Tested in              |
| ------------------------------------ | ---------------------------------------------------------------------------------------------------------------------- | ---------------------- |
| `formatRelativeTweetTime(iso, now?)` | x.com-style: now/`{n}m`/`{n}h`/`Mon d`/`Mon d, yyyy` using **local** date parts; null on unparseable. `now` injectable | `relativeTime.test.ts` |

## src/api (`anthropic.ts`) — imported ONLY by `entrypoints/background/generation.ts`

| Export                                      | Contract                                                                                                                                                                                                                                                                                                                                                                                                                              | Tested in           |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- |
| `callAnthropic(req)` → `MessagesCallResult` | Hand-rolled fetch to `/v1/messages` (no SDK — readability is the point). Key in `x-api-key` header only; `anthropic-version: 2023-06-01`; browser-access header; 60 s abort. Non-empty `system` → cache_control array (real savings only above per-model minimum). Discriminated result, never throws; `kind ∈ auth\|rate-limit\|network\|server\|bad-request\|other`; first text block extracted, empty text is the caller's problem | `anthropic.test.ts` |
| `verifyKey(apiKey, model)`                  | 8-token probe; HTTP success = key works                                                                                                                                                                                                                                                                                                                                                                                               | `anthropic.test.ts` |

## src/storage

| Module                   | Exports                                                                                                                                                                                                                                                                                            | Notes                                                                                                                                                                                                                                                                     | Tested in                                                             |
| ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| `config.ts`              | `getSettings`, `setSettings`, `subscribeSettings`                                                                                                                                                                                                                                                  | Per-field merge over `DEFAULT_SETTINGS`; legacy single-body or blanked templates reset to defaults (v1→v2 template migration); one-time idempotent default-chip seeding via `seededChipIds` (deleted seeds stay deleted); read-merge-write, one writing surface per field | `config.test.ts`                                                      |
| `key.ts`                 | `getApiKey` (background-only caller!), `hasApiKey`, `setApiKey`, `migrateApiKey`, `clearApiKey`                                                                                                                                                                                                    | Key isolated off `Settings` on purpose; UI is write-only — see `architecture.md` security boundary                                                                                                                                                                        | — (boundary pinned by SECURITY-AUDIT + anthropic tests)               |
| `corpus.ts`              | `openCorpus`, `addItem` (rejects dup id), `updateItem`, `deleteItem`, `clearAllItems` (items + bundles, ONE tx), `getAllItems`, `countItems`, `DB_NAME`, `DB_VERSION`, `STORE_BUNDLES`, `EXPORT_SCHEMA_VERSION`, `promisifyRequest` (shared by sibling store modules), `_resetCorpusCache` (tests) | Versioned IDB (v5 adds the `bundles` store); migration pattern in `architecture.md`                                                                                                                                                                                       | `corpus.test.ts` (incl. seeded v1→v3, v2→v3, v3→v4, v4→v5 migrations) |
| `bundles.ts`             | `addBundle` (rejects dup id), `updateBundle`, `deleteBundle`, `getBundle` (null on unknown), `getAllBundles`                                                                                                                                                                                       | CRUD over the v5 `bundles` store; same DB connection (`openCorpus` owns versioning). Dumb on purpose — member resolution lives in `lib/bundles`                                                                                                                           | `bundles.test.ts`                                                     |
| `captureMode.ts`         | `getCaptureMode`, `setCaptureMode`, `subscribeCaptureMode`, `ActiveCaptureMode`                                                                                                                                                                                                                    | One value = mutual exclusion of the two modes by representation                                                                                                                                                                                                           | —                                                                     |
| `captureBundleTarget.ts` | `getCaptureBundleTarget`, `setCaptureBundleTarget`, `subscribeCaptureBundleTarget`                                                                                                                                                                                                                 | The capture-mode bundle target — survives mode toggles (the bundle choice holds across off/on browsing) but the background clears it on last-panel close, so a fresh panel files into plain library; the background validates at capture time                             | —                                                                     |
| `replyContextLock.ts`    | `getReplyContextLock`, `setReplyContextLock`, `subscribeReplyContextLock`                                                                                                                                                                                                                          | Preserved across SPA navigation by design (highlight hides; context stays usable)                                                                                                                                                                                         | —                                                                     |
| `lastPrompt.ts`          | `getLastPrompt`, `setLastPrompt`, `subscribeLastPrompt`, `LastPromptRecord`, `PromptCall`                                                                                                                                                                                                          | `lastPrompt:v2` — structured per-call records `{label, system, user}[]`; written only by the pipeline; feeds the inspector                                                                                                                                                | —                                                                     |
| `theme.ts`               | `getThemePreference`, `setThemePreference`, `subscribeTheme`, `resolveTheme`, `bindDocumentTheme`                                                                                                                                                                                                  | Binary light/dark on `<html data-theme>`                                                                                                                                                                                                                                  | —                                                                     |
| `autoReplyFlag.ts`       | `setAutoReplyFlag(at)`, `consumeAutoReplyFlag()` (read-and-clear)                                                                                                                                                                                                                                  | One-shot shortcut handoff; stamp shared with the broadcast for dedupe                                                                                                                                                                                                     | —                                                                     |

All `subscribe*` helpers fire immediately with the current value, then on changes; they return an unsubscribe.

## src/messaging

| Export                                                                                       | Contract                                                                                                                                                                                                            | Tested in |
| -------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- |
| `contracts.ts` — the five unions + `isMessageOfType`, `isBackgroundNotice`                   | Single source of truth for every message shape (table in `architecture.md`). Add a message = add it here first                                                                                                      | —         |
| `envelope.ts` — `sendToBackground`, `sendOneWay`, `broadcastNotice`, `onMessage`, `onNotice` | Request/reply + broadcast wrappers; `onMessage` converts throws to `bg:error`; `sendOneWay` is sync-throw-proof for orphaned content scripts; the one documented protocol-boundary cast lives in `sendToBackground` | —         |

## src/types

`LibraryItem` (type incl. `'thread'` + identity-bearing `segments`; nullable `embedding` — §8 seam; `engagement` reserved), `Bundle` (Phase 6), `Draft`/`PostDraft` (the exercised §8 seam — per-post `residualViolations`), `Settings`/`ChipPreset`/`PromptTemplate(Key)`, `DEFAULT_SETTINGS`/`DEFAULT_MODEL` (pinned, verified 2026-06), `RawCapture`/`RawThreadCapture`/`CaptureFailureReason`, `ReplyContext`/`GenerationRequest` (incl. `bundleId`, `targetCount`)/`RefineRequest`/`RefineKind` (incl. `repack`)/`GenerationResult` (kind + targetCount). Types only — plus the two default constants; no logic.

## entrypoints/background (shell — orchestration only)

| File            | Responsibility                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| --------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `index.ts`      | `defineBackground`: sidePanel behavior, command handler (open-first gesture rule), panel-port tracking (`openPanelPorts` + heartbeat listener), session-storage change pushes, the `onMessage` routing table. No business logic                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `generation.ts` | `runGeneration` / `runRefine` / `runVerifyKey` / private `runPipeline` + `processDraftText`. The only key/API code. Pipeline: ≤3 calls (initial + repair + reshape), every one a `RenderedPrompt` with a system voice anchor, labelled per-call `setLastPrompt`, returns `GenerationResult`. Thread mode parses the `---` format and runs autoFix/exclusions/counts PER POST (4096 maxTokens; count validated on generates/repacks only, nudge beyond ±1; repair folds the count line in when both fire). Bundle seeding as before; repack labels `refine (repack to ≈N)`; scoped refines (`RefineRequest.scope`, the `rewrite` kind) wrap the instruction via `buildScopedThreadInstruction`, count-guard the parse (mismatch = honest failure), extract the post, and continue as a single-post pipeline                                                                                   |
| `capture.ts`    | `handleCapturedTweet` (filter → classify → add → notices; bundle-target filing; 5s grace window; `duplicateOfSource`), `handleCapturedThread` (Phase 10: the walked spine persists as ONE thread item, id = root status id, same filter/dedupe/filing/grace paths, banner carries `threadSegmentCount`), `handleManualAdd(text, itemType, bundleId)` (incl. thread paste — `---` parsed, <2 segments = honest error; success/duplicate also broadcast `bg:save-result`, so the floating banner shows the outcome and retires any stale error), `handleShippedDraft(text, segments, mode, bundleId)` (Phase 4 save + Phase 6 auto-filing: appends the SAVED record id to the seeding bundle via `appendBundleMember`, broadcasts `bg:bundles-changed`; deleted bundle skips silently), `failureReasonToSaveResultKind` (only `no-tweet-under-cursor` stays silent), `replyContextFailureKind` |
| `tabs.ts`       | `X_HOSTS`, `pushToTabs` (fire-and-forget all x tabs), `sendCaptureNavToActiveTab` (panel-forwarded nav keypress → the ACTIVE X tab only), `requestReplyContextFromActiveTab` (composer round-trip + reply guards), `isActiveTabOnX` (via lib `isXPageUrl`; needs no "tabs" permission — non-X URLs are invisible and read as off-X), `focusOrOpenXTab`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |

## entrypoints/twitter.content

| File         | Responsibility                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Tested in                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------- |
| `index.ts`   | Wiring: alive-probe, state mirror (mode/lock/panelOpen + 30 s lease, plus X's own state: `aria-modal` probe + pathname watch feeding `lib/overlay` `decideOverlayVisibility`), hover/click/keyboard routing (interactive children pass through to X; selection clicks affirm the clicked article as the lock target immediately, kept while connected in the active layer; ↑/↓ step the hover cursor via `stepToAdjacentArticle` + Enter selects — mode-armed only, never in editable fields; `keyboardNavActive` arbitrates the inputs (hover is inert while the keyboard drives; only genuine pointer movement hands back), held keys step consistently with INSTANT scroll on repeats (smooth on discrete presses — animation can't keep repeat pace), the cursor resumes from the locked tweet in reply-context mode, a resume anchor re-acquires the last cursor position on the first press after a release/drift (only the next press moves), margin-based scrollBy nudges instead of recentering; Escape de-selects in stages gated on VISIBILITY — the visible cursor first, then the lock via the shared `dismissReplyContext` (a cursor sitting on the locked tweet paints nothing and is skipped, not a stage) — and yields to X's modals; keys arrive locally AND panel-forwarded via `bg:capture-nav` through one `handleCaptureNavKey`), rAF reposition loop with 200 ms throttled state scan, message guards | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `extract.ts` | Every read of X's markup: `extractTweet`, `extractReplyContextFromArticle`, `extractReplyContextFromComposer` (shortcut path), `findGrandparentArticle` (status/with_replies pages only — refuses to guess on feeds), `collectSelfThreadSpine` (Phase 10: up-walk to the root then the contiguous same-author run; foreign article/spacer stops it; reply-roots degrade to single; status-pages only, never throws), `extractThread` (spine → `RawThreadCapture` via the shared readers), `findArticleByStatusId(id, scope: 'page'                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | 'modal')`(page skips dialog-resident copies; modal searches only the`aria-modal`layer),`isXModalOpen` (`aria-modal`probe; fails open),`detectReplyByDomStructure`, `stepToAdjacentArticle(current, direction)`(arrow-key navigation: one article per call — acceleration reverted, the scroll couldn't keep pace — DOM-order, modal-layer-confined, viewport-anchored start, null at edges so the press scrolls natively and virtualization renders more),`readVisibleText`(emoji`<img alt>`), `readAuthorHandle`, `readDisplayName`, `readStatusId`, `readTimestamp`, `readAvatarUrl`(**pbs.twimg.com gate**),`detectReplyContext`, `isTweetTruncated`, `hasMedia` | `extract.test.ts` |
| `overlay.ts` | `createOverlaySystem` → preview/lock/label/dismiss elements + injected style tag; rect-cache repositioning; §6 carve-out rules enforced here                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

## entrypoints mains

`sidepanel/main.tsx` — React root + the `margin-panel` port with 20 s heartbeat and reconnect (MV3 keepalive; see `architecture.md`). `options/main.tsx` — React root only.

## src/ui (shell — render only; all text React-escaped, no HTML sinks)

| Component                                   | Responsibility / contract                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `App.tsx`                                   | `surface: 'panel' \| 'options'` switch. PanelShell: screen state, floating save-result / reply-context-error banners (mutually exclusive), toast plumbing, flash-row routing (`{id, kind: 'added'                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | 'locate'}`; `onLocateItem` hands bundle-member links the same reveal as the duplicate CTA), off-X overlay state (`bg:active-tab-state` → veil with Open x.com / Compose anyway). PanelHead + ThemeToggle |
| `ComposeScreen.tsx`                         | Owns composition state; composes `lib/draft`'s lifecycle reducer (dispatch-only — stale gating and undo scopes live in lib), the single timed-undo timer, the new-context decision (via lib `isSameTweet`), copy-commits + `emitDraftCommit` (incl. `seedBundleId` provenance → `panel:draft-committed`), the Done exit (clears draft/angle/lock together under the timed-undo toast; seed + mode survive), the bundle picker state + the per-draft filing override, thread mode state (`composeMode` + the ≈N target; stepper change over an active thread fires the repack same-tick; cap-flip refits per post), per-post copy (`copyPost`/`copyNext` — the ⌃⇧↵ shortcut targets the first uncopied post) with the corpus event fired at the committed TRANSITION, and the background round-trips; shortcut path merges re-deliveries via lib `mergeReplyContextSelection`                                                          |
| `compose/DraftEditor.tsx`                   | The directly-editable draft surface: real textarea + metrics-identical highlight backdrop (only while residual violations exist; first hand edit clears them via the lifecycle), auto-grow                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `compose/PreDraftState.tsx`                 | Input form before a draft exists. Props: `reply`/`brief`/`bundlePicker`/`threadControls` groups + busy/libraryCount/error/handlers; context + seed + post↔thread mode render as one grounding cluster above the angle; the button reads Generate post/reply/thread; with a bundle picked, the footer states its exact resolved member count                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `compose/DraftState.tsx`                    | Brief bar (expandable; context peek — "to @handle" / the Replying-to row reveal the full `ReplyContextCard`), draft card (count, over-cap warn, copy/regen/undo), chips + steer controls, inspector, the seeded-draft note, the "On copy" effects row, the refine-scope pill ('Aimed at post N' + clearing ×), the committed-state action swap (primary becomes the success-green "Done — next draft"; singles keep a copy-again button, thread cards their own), thread rendering (DraftView is kind + posts[]; threads render ThreadCards with a 'Copy 2/5' next-uncopied primary button and a copied-count head; singles unchanged as the N=1 case). Props: `reply`/`brief`/`draft: DraftView`/`refine`/`bundlePicker`/`seedBundleName`/`fileToBundle`/`threadControls`                                                                                                                                                            |
| `compose/SeedLine.tsx` (was BundlePicker)   | The voice-seed line + popover menu (reskin): "Sounding like your library · N examples" / "Sounding like “bundle” · N members"; menu rows state sampled vs in-order-exact with honest missing counts and the ★-guaranteed clause; footer carries the no-sampling rule. Always rendered (zero bundles = the library row alone)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `compose/ModeControls.tsx`                  | Post↔Thread as a b/w mini-seg in its own mode bar + the ≈N stepper (bounds 2–9; `compact` renders the stepper alone for the thread bar); hidden while reply context exists (posts-only v1)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `compose/CountRing.tsx`                     | The X-style 22px count ring (blue arc / red over / green committed)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `compose/monogram.ts`                       | One-letter avatar monogram from the configured handle                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `compose/ViolationNote.tsx`                 | Explains the highlight marks (via lib `describeViolations`): which of the user's rules broke, that the repair pass already ran, both exits (hand-edit clears the marks / regenerate). The one place violations reach screen readers — the backdrop is aria-hidden. Hides with the marks                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `compose/ThreadCards.tsx`                   | A thread draft as ordered cards — each a `DraftEditor` with ordinal, per-post weighted count, its own copy button (sticky check via the lifecycle's per-post `copied`), a Rewrite (scoped fresh take), and an Aim toggle (points chips + steer at that post; accent ring while aimed)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `compose/ReplyContextCard.tsx`              | Captured tweet rendered X-native (avatar/name/time/grandparent/media note) + clear; optional `onCollapse` turns its header into the peek's collapse control (one header, two sizes); long text clamps X-style (6 lines, grandparent 3) with in-place Show more/less — display-only, the prompt gets the full text; re-collapses when the lock swaps                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `compose/ReplyContextBanner.tsx`            | Reply-context mode toggle (full + `compact`)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `compose/ErrorCard.tsx`                     | `ErrorKind` → tone/title/copy/action map (settings vs retry)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `compose/CapToggle.tsx`, `compose/types.ts` | ≤280 switch; shared `ReplyContextControls`/`BriefControls`/`BundlePickerControls`/`BundleOption` prop groups                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `VoiceScreen.tsx`                           | Two collapsible sections (Bundles / Saved examples) sharing the `.sec-head` header pattern. Library: filter pills (incl. Threads, visible once any thread exists) + live search (visible = pill ∩ query via lib `matchesSearch`; pill counts are query-faceted while starred-pill visibility/reset follow the library; Escape clears; query-aware empty state), expand/collapse, remove-with-undo-toast, inline patch, manual add (+optional bundle target), locate-flash opens the section + widens the filter + clears the query. Bundles: list state (+`bg:bundles-changed`), selection mode (pickedIds in SELECTION order = stored member order; destination = new-or-existing bundle, with member picks pruned on retarget; starting a pick opens both sections), save (empty new bundles are valid)/append/rename/prune/reorder (via lib `moveBundleMember`)/delete-with-undo handlers + `createEmptyBundle` for inline minting |
| `voice/CaptureBanner.tsx`                   | "Save tweets from X" mode toggle (reads/writes captureMode) + the shared "Also file into" target select (reads/writes captureBundleTarget; survives mode toggles — re-picking per flip was the friction — resets on panel close and when its bundle disappears; shown whenever capture is on — minting matters most at zero bundles)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `voice/LibRow.tsx`                          | One saved example: star toggle (manual/shipped only — the Concept A boundary), clamp + show more, inline edit (text/type), delete; scrolls itself into view on a `dup` flash ("Show me"); optional `selection` prop swaps actions for a numbered pick control (whole row toggles; `locked` = inert check); thread rows wear `thread · N`, render via `ThreadText`, and edit via `ThreadEditor`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `voice/BundleSection.tsx`                   | The Bundles section (header + pick bar + height-bounded list): the pick bar targets a new bundle (name only — empty is valid) or an existing one (destination select; each row's + starts pre-targeted); rows expand to members (locate link, ↑/↓ reorder, prune ×), inline rename, delete; honest counts via `resolveBundleMembers` ("· N missing"); the + entry point renders even at zero bundles                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `voice/AddForm.tsx`                         | Manual paste + "this is my own writing" gate (replaces validateAuthor for this path) + the shared "Also file into" target select + the Thread type (segments separated by a `---` line)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `voice/ThreadRow.tsx`                       | `ThreadText` (collapsed = first post clamped, expanded = connected segment blocks with ordinals) + `ThreadEditor` (one textarea per post; text edits only in v1; saving rejoins text and keeps segment ids)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `voice/BundleTargetSelect.tsx`              | The shared "file into a bundle" select (capture banner + paste form): existing bundles plus "+ New bundle…", which swaps in an inline name field and mints an empty bundle via the owner's callback                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `OptionsPage.tsx`                           | Section nav (account/rules/prompts/data), saved-flash, theme button                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `sections/AccountSection.tsx`               | Handle (blur-save) + **write-only key field** (Save keeps-on-blank / Clear removes both areas / Verify checks the SAVED key, disabled while dirty) + read-only model line                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `sections/OutputRulesSection.tsx`           | Structural toggles, banlist (blur-save), pool-size + guaranteed-stars + curated/archive-balance sliders (balance disabled until archive items exist) + temperature pair (with regen≤gen hint)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `sections/PromptsSection.tsx`               | Template groups + chip editor shell                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `sections/prompts/TemplateRow.tsx`          | Collapsible editor — one textarea per body (System / User), each saved on blur; slot badges across both via `extractSlotNames`/`validateTemplate`; reset-to-default                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `sections/prompts/ChipEditor.tsx`           | Chip rows; text edits commit on blur, structural changes immediately                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `sections/DataSection.tsx`                  | Export JSON (`EXPORT_SCHEMA_VERSION`; carries `items` + `bundles` since v5), clear-all (items + bundles, one transaction, two-step confirm)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `LastPromptInspector.tsx`                   | Renders every call in `lastPrompt:v2` as a per-call accordion (single call opens itself; chains start collapsed), Response always visible, copy buttons; no string splitting, the stored fields are what was sent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `SaveResultBanner.tsx`                      | Seven-kind capture-result banner (`SAVE_META` + copy per kind); states bundle filing ("Filed into …") when the capture target applied; duplicate wording follows `duplicateOfSource` (a shipped match reads as the promotion it is); thread captures read 'Saved as a thread · N posts' with the honest rendered count (manual thread pastes get the exact count, no scroll caveat). Manual adds broadcast into the same banner, so a fresh save outcome always supersedes a lingering one                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `ReplyContextErrorBanner.tsx`               | Reply-context failure wording in the same banner chrome                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `Toast.tsx`                                 | Bottom pill, optional action; caller owns dismissal timing                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `Avatar.tsx`                                | `<img>` with **pbs.twimg.com render gate**, `no-referrer`, initials fallback                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `highlights.tsx`                            | `renderWithHighlights(text, spans)` → text + `<mark class="hl hl-<rule>">` React nodes; overlap-safe; the only violation-rendering path                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `BrandMark.tsx`, `icons.tsx`                | CSS brand mark; 24×24 stroke icon set                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

## Adding a module — checklist

1. Pure logic → `src/lib/<area>/`, exported via the area's `index.ts`, with a `*.test.ts` beside it.
2. New message → `contracts.ts` union first, then route in `background/index.ts`, then guards if content-bound.
3. New stored value → its own `src/storage/<thing>.ts` following the get/set/subscribe pattern, `:vN`-suffixed key, re-export from `storage/index.ts`.
4. New UI piece → smallest sensible component in the owning folder (`compose/`, `voice/`, `sections/`); screens own state, children render.
5. Add a row HERE, in the same commit.
