# Security audit

Verification of the codebase against the hard rules in [`CLAUDE.md`](./CLAUDE.md) §6.

**Audited tree:** branch `feat/context-bundles` (Phase 6). Boundary checks 1–6, 11, and 13 re-run after the messaging/generation changes — every claim below held unchanged (one `getApiKey` caller, one `fetch`, zero injection sinks, no key on any message, content script clean). Prior stamp: `chore/codebase-hardening`, post-remediation (Phase 2 of the 2026-06 hardening pass; see `AUDIT.md`).
**Date:** 2026-06-11.
**Staleness rule:** this document describes the tree at the commit that last touched it. If file paths it cites don't exist, the audit is stale — re-run it (every check below is a one-command grep or a build inspection) and re-stamp it. Re-running this audit is part of the release checklist.

## Method

Programmatic `grep` over `src/` and `entrypoints/`, manual review of every import boundary, and inspection of the production bundle (`.output/chrome-mv3/`) to confirm nothing extra leaked through the bundler. Test files are excluded from boundary greps where noted — the §6 invariants concern extension runtime contexts; tests stub the network and never hold a real key.

## Findings — all checks PASS

| #   | Invariant                                  | Method                                                                                               | Result                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| --- | ------------------------------------------ | ---------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | API key value read only by the background  | `grep -rn getApiKey src/ entrypoints/` (excl. its own module/barrel/tests)                           | Exactly one calling file: `entrypoints/background/generation.ts`. The options page (`AccountSection.tsx`) is **write-only** — it uses `hasApiKey` (boolean), `setApiKey`, `migrateApiKey`, `clearApiKey`, and never reads the value back into page state or the DOM.                                                                                                                                                                                           |
| 2   | `src/api/` imported only by the background | `grep -rn "from '.*api/anthropic'"`                                                                  | Two importers: `entrypoints/background/generation.ts` (the runtime caller) and `src/api/anthropic.test.ts` (its own test, which stubs `fetch` and uses a fake key). No content-script or panel import.                                                                                                                                                                                                                                                         |
| 3   | Storage is `local`/`session`, never `sync` | `grep -rn "storage\.sync"`                                                                           | Single hit is the explanatory comment in `src/storage/config.ts` — no usage.                                                                                                                                                                                                                                                                                                                                                                                   |
| 4   | Only `api.anthropic.com` is reached        | `grep -rn "fetch("` (excl. tests)                                                                    | One call site, `src/api/anthropic.ts`, targeting `https://api.anthropic.com/v1/messages`, with a 60 s abort.                                                                                                                                                                                                                                                                                                                                                   |
| 5   | No telemetry / analytics endpoints         | `grep -rin 'analytics\|telemetry\|posthog\|mixpanel\|segment\|sentry\|datadog\|fathom\|plausible'`   | Hits are the options-page footer copy ("no telemetry") and the substring `segment` inside `segmentSentences` (the staccato detector). No analytics code, no endpoints.                                                                                                                                                                                                                                                                                         |
| 6   | No DOM injection sinks                     | `grep -rn "innerHTML\|outerHTML\|insertAdjacent\|executeScript"`                                     | Zero hits anywhere — including tests (fixtures are built via `DOMParser`).                                                                                                                                                                                                                                                                                                                                                                                     |
| 7   | No auto-post; clipboard is the only output | `grep -rn clipboard` + manual review                                                                 | `navigator.clipboard.writeText` in `ComposeScreen.tsx` (the Copy button) and `LastPromptInspector.tsx` (copy prompt/response) — both user-click-initiated, both in the panel. Nothing writes to X's composer or triggers posting.                                                                                                                                                                                                                              |
| 8   | Overlay carve-out compliance (§6)          | `grep -c data-margin-overlay entrypoints/twitter.content/overlay.ts` + manual review of `overlay.ts` | Every extension-rendered element carries `data-margin-overlay` (16 occurrences in `overlay.ts`, one grep finds them all). Root and visuals are `pointer-events: none`; the dismiss button is the only `pointer-events: auto` element and its handler sends only `content:dismiss-reply-context` (clears extension-side state). Nothing is written onto X's own elements. Overlays hide on panel-close, mode-off, dismiss, and drop off-page on SPA navigation. |
| 9   | Permissions minimal, no `<all_urls>`       | Built `manifest.json` review                                                                         | Permissions: `storage`, `sidePanel`, `clipboardWrite`, `unlimitedStorage`. Host permissions: the four x.com/twitter.com origins. `minimum_chrome_version: 116`. Default MV3 CSP (no weakening), content script in the default isolated world, no `externally_connectable` (web pages and other extensions cannot message the worker).                                                                                                                          |
| 10  | Key never logged                           | `grep -rn "console\."` then inspect each                                                             | Two `console.error` calls, both in `entrypoints/background/index.ts`, both for sidePanel failures. Neither has the key in scope. `anthropic.test.ts` additionally pins that error messages never echo the key.                                                                                                                                                                                                                                                 |
| 11  | Key not on messaging payloads              | `grep -n "apiKey\|key:" src/messaging/`                                                              | Zero hits. The key travels only inside the background, in the `x-api-key` request header (pinned by test).                                                                                                                                                                                                                                                                                                                                                     |
| 12  | Key not in `lastPrompt` storage            | Manual review of `src/storage/lastPrompt.ts` + the pipeline                                          | The record stores the rendered prompt body and response text; the key never enters prompt text.                                                                                                                                                                                                                                                                                                                                                                |
| 13  | Content script has no key/api dependency   | `grep -rn "api/anthropic\|storage/key\|getApiKey" entrypoints/twitter.content/`                      | Zero hits.                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 14  | Built bundles confirm the boundaries       | `grep -ohE 'https?://[a-z0-9.-]+' .output/chrome-mv3/{background.js,content-scripts/twitter.js}`     | `api.anthropic.com` + the four x/twitter hosts + `twemoji.maxcdn.com` (see note below). The content-script bundle contains zero occurrences of `api.anthropic.com`, `x-api-key`, or `apiKey`.                                                                                                                                                                                                                                                                  |
| 15  | Avatar images limited to `pbs.twimg.com`   | Manual review of `readAvatarUrl` (extract.ts) and `src/ui/Avatar.tsx`                                | Enforced twice: at capture (only `https://pbs.twimg.com/` sources are stored) and at render (`Avatar` falls back to initials for any other host — covers anything that reaches IndexedDB by another path, e.g. a future import). `referrerPolicy="no-referrer"` on the `<img>`.                                                                                                                                                                                |

## Note on `twemoji.maxcdn.com`

The string `https://twemoji.maxcdn.com/...` appears in the built `background.js` because the transitive dep `twemoji-parser` (used by `twitter-text` for emoji-position detection) builds emoji image URLs _if_ its caller requests them. We call `parseTweet` only for `weightedLength`; the URL-building path is never invoked. Even if a future regression in the dep started invoking it, Chrome would block the request — `host_permissions` does not include that host, and an extension cannot reach an undeclared host. Investigated and ruled a non-issue; rewriting `twitter-text` to strip the constant is not worth the fork.

## Dependency audit policy

`npm audit --omit=dev`: **0 vulnerabilities** (production deps are `react`, `react-dom`, `twitter-text`). The full `npm audit` reports findings confined to the dev-only `wxt → web-ext-run` toolchain (the browser launcher used by `npm run dev`); they do not ship in the extension and are tracked against upstream releases. Policy: the production audit must be clean before any release; dev-only findings are tracked, not force-fixed.

## Conclusion

**No exceptions to the §6 invariants.** The architecture and its build output match the rules end to end: the key value is readable by exactly one file (the background's generation module), the single external endpoint is `api.anthropic.com`, x.com contact is read-only plus the audited overlay carve-out, and there is no proxy, telemetry, or third-party endpoint of any kind.
